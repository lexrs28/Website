# Runbook: Dictator Game Operations (Deprecated)

This runbook is retained as historical context only.

- Canonical experiment route is now `/experiments/temporal-discounting`.
- Canonical API routes are now:
  - `POST /api/experiments/temporal-discounting`
  - `GET /api/experiments/temporal-discounting/export`
- Current operations guide: `docs/runbooks/temporal-discounting-ops.md`.

Legacy compatibility wrappers still exist for transition support:
- `POST /api/experiments/dictator-game`
- `GET /api/experiments/dictator-game/export`
